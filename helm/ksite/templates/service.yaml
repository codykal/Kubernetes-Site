apiVersion: v1
kind: Service
metadata:
  name: {{ include "ksite.fullname" . }}
  labels:
    {{- include "ksite.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ksite.selectorLabels" . | nindent 4 }}
